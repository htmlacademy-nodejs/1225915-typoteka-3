extends ../layout

block constants
  - const role = `author`;
  - const bodyClass = `wrapper wrapper--nobackground`;
  - const footerClass = `footer footer--padding`;

block main
  main.main-page.main-page--padding
    section.main-page__publication.publication
      h1.publication__title Комментарии
      ul.publication__list
        each comment of comments
          li.publication__list-item
            .publication__header
              img.publication__list-image(src=`/img/${comment.author.avatar}` width='20' height='20' alt='Аватар пользователя')
              b.publication__list-name #{comment.author.name}
              time.publication__item-time(datetime=comment.createdAt) #{comment.formattedCreatedAt}
            a.publication__item-text(href=`/articles/${comment.article_id}`) #{comment.title}
            p.publication__text-strong #{comment.text}
            button.publication__button.button.button--close-item(type='button')
              span.visually-hidden Закрыть строку списка
